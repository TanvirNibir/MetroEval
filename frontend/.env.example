# =============================================================================
# MetroEval Frontend - Environment Variables Configuration
# =============================================================================
# Copy this file to .env and fill in your actual values
# DO NOT commit .env to version control - it contains sensitive information
# =============================================================================

# -----------------------------------------------------------------------------
# API Configuration
# -----------------------------------------------------------------------------

# Backend API URL
# For development: Leave empty or use http://localhost:5001/api
# For production: Set to your production backend URL (e.g., https://api.yourdomain.com/api)
# Note: If not set, the app will use relative paths (/api) which works for same-domain deployments
VITE_API_URL=

# Alternative: React App API URL (for compatibility)
# Only needed if VITE_API_URL doesn't work with your build setup
REACT_APP_API_URL=
